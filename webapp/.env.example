# Copy to webapp/.env.local (Next.js convention) or webapp/.env for tooling.
# Only expose values prefixed with NEXT_PUBLIC_ to the browser.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_AI_GATEWAY_BASE_URL=http://localhost:8010
